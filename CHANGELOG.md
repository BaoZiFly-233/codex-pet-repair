# 更新记录

## 1.2.0 — 2026-09-08

- 新增「看向鼠标」开关，让 V2 宠物随鼠标移动改变视线，并提供配套的 Codex 启动入口。
- 改善自动修复与鼠标跟随的配合，减少宠物移动或尺寸变化时的重复修复。
- 日志增加跟随状态和连接失败原因，方便排查问题。
- 清理重复代码，略微减小程序体积。
- 下载包不再附带说明文档，保留程序和必要的许可文件。

## 1.1.0 — 2026-09-07

- 设置项改用带文字的标准 Slint Switch，消除重复点击入口；宽窗口两列等宽，窄窗口改为一列。
- 默认窗口在状态与布局更新后按内容调整高度；手动调整窗口大小后保持用户尺寸，空间不足时允许滚动。
- 修复卡片、普通按钮与加载条被拉高的问题；日志菜单和居中的组件许可弹层使用标准组件。
- 删除旧手绘界面；预览和截图导出改为默认关闭的开发功能。
- 窗口事件按 HWND 增量核验，以固定 300 毫秒期限合并；完整发现采用独立兜底定时器。
- 缓存验证过的进程身份与句柄，修复事务中复用句柄；修改前和恢复时仍完整核验身份。
- 界面通过事件通知同步轻量状态，保留 10 秒兜底；操作失败回写实际设置。
- 保持设置、确认过的 Codex 版本、待恢复事务格式兼容；确认有效不会关闭已开启的自动修复。
- 下载包仅保留程序、致谢与第三方许可；说明文档和预览图保留在源码仓库。

实际 Windows 呈现验证受本机 Computer Use 截图接口限制，详见验证记录。本版本未改变原有不透明主窗口和按需整帧提交补丁。
