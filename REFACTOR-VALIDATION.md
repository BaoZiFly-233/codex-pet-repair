# 1.1.0 验证记录

日期：2026-09-07。环境：Windows 10 Pro 22H2，构建 19045.6466；Rust 1.94，x64 MSVC；Slint 锁定 1.17.1。

## 已通过

- 核心 17 项单元测试、UI 3 项单元测试；两端 Clippy `-D warnings` 通过。
- 核心与 UI 完成 release 构建。分发 UI 使用默认 feature，不启用开发预览与截图导出。
- 原生自测覆盖严格分类、正常恢复、取消、并发样式变更、辅助进程崩溃、窗口销毁、父进程退出和事务恢复，共 8 个场景。自测操作自建窗口，不代表真实宠物拖动效果已经确认。
- UI 通信测试覆盖通知合并、失败响应中的实际设置保留，以及命名管道读取超时取消后继续使用。
- 布局脚本 14 个场景通过：100%、150%、200% 缩放分别检查默认、深色、修复中、异步追加状态，再检查 320×260 和 1200×560 手动尺寸。

## 默认滚动修复

此前内容适配早于最终状态布局，后续状态增高没有重新适配；高 DPI 下再次设置窗口时还混用了逻辑尺寸和物理尺寸。现在内容高度变化后触发一次性适配，统一换算为物理尺寸；不增加空闲刷新循环。用户手动调整大小后保留用户尺寸，必要时滚动。

| 场景 | 实测逻辑尺寸（DIP） | 滚动溢出 |
| --- | --- | --- |
| 默认、深色 | 432×439；150% 时高度 439.33 | 0 |
| 修复中 | 432×450 | 0 |
| 异步追加状态 | 432×479；150% 时高度 478.67 | 0 |
| 手动宽窗口 | 1200×560 | 0 |
| 手动最小窗口 | 320×260 | 356 DIP，预期可滚动 |

已查看浅色、深色、修复中应用内渲染图；README 中的预览图来自本次构建。

## 验证限制

- 本机 Computer Use 截图接口返回 `0x80004002`，实际 Windows 呈现、遮挡恢复、焦点、菜单交互和 DPI 显示验收尚未完整完成。上述离屏布局检查不能替代实际呈现验收。
- 本轮未完成同环境三轮、每轮 60 秒的全部进程性能对比，不据此宣称 CPU 或内存占用下降。复测方法见 DEVELOPMENT.md。
- 保留原有不透明主窗口、按需整帧提交及身份核验保护。真实客户端效果仍通过修复后的用户确认入口确认。

## 分发

打包脚本仅复制程序、CREDITS 和许可文件，并按锁定依赖生成第三方许可。说明文档、预览图和补丁说明保留在源码仓库。下载包不包含用户设置、日志或开发预览程序。解压字节数和 ZIP 字节数在构建交付时分别报告。
